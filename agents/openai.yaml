interface:
  display_name: "自适应多代理路由 V2"
  short_description: "按契约路由，并公开核验每个子代理的模型、强度与回执"
  default_prompt: "使用 $adaptive-agent-routing 处理以下任务，按 V2 契约判断是否委派，并逐个公开子代理的模型、推理强度、派发回执与结果：<任务>"
policy:
  allow_implicit_invocation: false
